# Config for air
root = "."
tmp_dir = "tmp"                   # Temp dir for .air files (default "tmp")
bin = "tmp/main.exe"              # Binary file where your main.go compiled to

[build]
  # cmd = "go build -o tmp/main.exe ."
  cmd = "go build -o tmp/main.exe main.go"  # 只编译 main.go
  bin = "tmp/main.exe"
  include_ext = ["go", "tmpl"]    # Files to watch
  exclude_dir = ["cmd", "vendor", "common\\extend\\i18n\\locales", "storage", "tests"] # Excluded directories
  exclude_file = ["cmd/cli.go"]

[log]
  skip_dir = ["assets"]           # Directories to skip printing logs